
// features/guitar-tuner/guitarTuner.sample.ts
// This utility is primarily interactive and doesn't store complex sample data in AppData.
export const sampleGuitarTunerData = {
  // e.g., if we had default tuning profiles or calibration data
  // it could be part of a 'guitarTunerSampleState' object here.
};
