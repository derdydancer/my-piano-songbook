
// features/docs-viewer/docsViewer.sample.ts
// This utility is primarily for displaying existing doc files.
// It doesn't have specific data to store in the sample AppData structure.
export const sampleDocsViewerData = {
  // e.g., if we had a setting like 'defaultDoc': 'FeatureOrganization.md'
  // it could be part of a 'docsViewerSettings' object here.
};
